{% extends 'GPGestionBundle::layout-default.html.twig' %}

{% block incontent %}
	<a href="{{ path("gb_gestion_bundle_parametrage_clients_new") }}" class="pull-right btn btn-success" >Nouveau</a>

	<h1 class="page-header">Liste des clients</h1>

	<div class="table-responsive">
		<table class="table table-striped">
			<thead>
				<tr>
					<th>Entreprise</th>
					<th>Nom</th>
					<th>Ville</th>
					<th>Pays</th>
					<th>Id</th>
				</tr>
			</thead>
			{% if items|length > 0 %}
			<tbody>
				{% for item in items %}
				<tr>
					<td>
						<a href = "{{ path("gb_gestion_bundle_parametrage_clients_edit", { 'id': item.id }) }}">
							{{ item.entreprise }}
						</a>
					</td>
					<td>{{ item.nom }}</td>
					<td>{{ item.ville }}</td>
					<td>{{ item.pays }}</td>
					<td>{{ item.id }}</td>
				</tr>
				{% endfor %}
			</tbody>
			{% endif %}
		</table>
	</div>	
{% endblock %}